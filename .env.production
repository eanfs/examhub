# 生产构建环境变量（Vite 构建期注入，会打进镜像 —— 请勿放任何密钥）
# 数据源固定为真实接口
VITE_DATA_SOURCE=api
# 接口走同源 /api，由容器内 nginx 反代到后端（见 docker/exam-web.conf）
VITE_API_BASE=/api
# 初始考试 ID 留空：App 启动后由考试列表接口回退到列表首项
VITE_EXAM_ID=
# 鉴权 token 不在构建期硬编码（会过期、且属密钥）。
# 生产鉴权应由后端网关 / 同源 cookie 处理，或在 CI 构建期通过变量注入。
VITE_API_TOKEN=
